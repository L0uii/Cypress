import { LabelValuePipe } from './label-value.pipe';

describe('LabelValuePipe', () => {
  it('create an instance', () => {
    const pipe = new LabelValuePipe();
    expect(pipe).toBeTruthy();
  });
});
